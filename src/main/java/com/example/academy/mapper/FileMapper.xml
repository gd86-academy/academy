<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.academy.mapper.FileMapper">
	
	<insert id="insertFile" parameterType="com.example.academy.vo.File" useGeneratedKeys="true" keyProperty="fileNo">
		INSERT INTO file(
			file_name
			, file_ext
			, file_origin
			, file_size
			, file_category
		) VALUES(
			#{fileName}
			, #{fileExt}
			, #{fileOrigin}
			, #{fileSize}
			, #{fileCategory}
		)
	</insert>

</mapper>